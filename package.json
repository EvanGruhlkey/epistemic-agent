{
  "name": "duck-hotline",
  "version": "0.1.0",
  "private": true,
  "description": "Root scripts only — dependencies live in web/. Avoids npm workspaces so Next can patch lockfiles and install SWC correctly on Windows.",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix web",
    "lint": "npm run lint --prefix web",
    "typecheck": "npm run typecheck --prefix web",
    "check": "npm run lint --prefix web && npm run typecheck --prefix web && npm run build --prefix web"
  },
  "engines": {
    "node": ">=20"
  }
}
