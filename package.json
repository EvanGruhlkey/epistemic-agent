{
  "name": "duck-hotline",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w web",
    "start": "npm run start -w web",
    "lint": "npm run lint -w web",
    "typecheck": "npm run typecheck -w web",
    "check": "npm run lint -w web && npm run typecheck -w web && npm run build -w web"
  },
  "engines": {
    "node": ">=20"
  }
}
